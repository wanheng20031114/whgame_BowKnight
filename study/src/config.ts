/**
 * 配置常量
 */

export const GRID_CONFIG = {
    rows: 6,          // 行数
    cols: 8,          // 列数
    cellSize: 90,     // 格子大小（像素）
    gap: 4,           // 格子间距（像素）
    offsetX: 50,      // 网格起始 X 位置
    offsetY: 140,     // 网格起始 Y 位置
};

export const APP_CONFIG = {
    width: 1280,
    height: 720,
    backgroundColor: 0x1a1a2e,
};
